[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "ntechdevelopers"
authors = [{name = "Ntech", email = "contact@ntechdevelopers.com"}]
dynamic = ["version", "description"]
